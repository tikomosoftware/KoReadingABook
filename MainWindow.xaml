<Window x:Class="KoReadingABook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Reading a Book" Height="250" Width="300"
        Icon="Resources/app-icon.ico"
        MinHeight="250" MinWidth="300"
        WindowStartupLocation="Manual"
        Background="#F3F4F6" FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Status: " FontWeight="Bold" FontSize="16" Foreground="#374151"/>
                <TextBlock x:Name="StatusTextBlock" Text="Waiting..." FontSize="16" Foreground="#6B7280"/>
                <TextBlock x:Name="NextRunTextBlock" Text="" Margin="20,0,0,0" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Log Area -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
            </Border.Effect>
            <ListView x:Name="LogListView" BorderThickness="0" FontFamily="Consolas">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Time" Width="140" DisplayMemberBinding="{Binding Timestamp}"/>
                        <GridViewColumn Header="Type" Width="80" DisplayMemberBinding="{Binding Type}"/>
                        <GridViewColumn Header="Message" Width="300" DisplayMemberBinding="{Binding Message}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Interval:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="#4B5563"/>
                <ComboBox x:Name="IntervalComboBox" Width="60" SelectionChanged="IntervalComboBox_SelectionChanged" VerticalContentAlignment="Center">
                    <ComboBoxItem Content="10s" Tag="10"/>
                    <ComboBoxItem Content="15s" Tag="15"/>
                    <ComboBoxItem Content="20s" Tag="20"/>
                    <ComboBoxItem Content="30s" Tag="30"/>
                </ComboBox>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ClearButton" Content="Clear" Click="ClearButton_Click" Background="#3B82F6" Foreground="White" BorderThickness="0" Width="60"/>
                <Button x:Name="StartButton" Content="Start" Click="StartButton_Click" Background="#10B981" Foreground="White" BorderThickness="0" Width="60"/>
                <Button x:Name="StopButton" Content="Stop" Click="StopButton_Click" Background="#EF4444" Foreground="White" BorderThickness="0" Width="60" IsEnabled="False"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
